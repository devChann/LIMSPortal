@*@model List<Person>
<h3>Best sellers</h3>
<ul style="padding-left:0;list-style-type:none;">
    @foreach (var person in Model)
    {
        <li>@person.FName - @person.SName (@person.number.ToString("c"))</li>
    }
</ul>*@