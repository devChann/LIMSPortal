@model IEnumerable<LIMS.Core.Entities.Responsibility>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PerformanceRequirement)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResponsibilityType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PerformanceRequirement)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResponsibilityType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ResponsibilityId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ResponsibilityId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ResponsibilityId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
