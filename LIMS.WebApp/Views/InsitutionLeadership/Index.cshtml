@model IEnumerable<LIMS.Core.Entities.InsitutionLeadership>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MemberSince)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MemberUntil)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MembershipRole)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MembershipStatus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MemberSince)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MemberUntil)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MembershipRole)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MembershipStatus)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
