@model IEnumerable<LIMS.Core.Entities.Leasehold>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LeasePeriod)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lessor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tenure)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LeasePeriod)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lessor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tenure.TenureId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LeaseholdId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LeaseholdId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LeaseholdId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
